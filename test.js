for (let index = 0; index < 4; index++) {
    var number
    const element = array["Enter a number", "The total is " + number];
    number = window.prompt(array[1])
    total = number + total
}
alert(array[2])